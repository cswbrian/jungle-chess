# PeerJS signaling server hostname (without https:// or port)
# Deploy the peerjs-server/ directory to Render (free), then paste the hostname here.
# Example: jungle-chess-peerjs.onrender.com
# Leave empty to use the default PeerJS cloud (works locally but unreliable on production).
VITE_PEERJS_HOST=
